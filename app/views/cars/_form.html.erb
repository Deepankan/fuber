<%= form_for(@car) do |f| %>
  <% if @car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% @car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :is_pink %><br>
    <%= f.check_box :is_pink %>
  </div>
  
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address, placeholder: "Please Enter full address" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
